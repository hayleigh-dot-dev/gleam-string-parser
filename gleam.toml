name = "string_parser"
version = "1.1.0"

# Fill out these fields if you intend to generate HTML documentation or publish
# your project to the Hex package manager.
#
# licences = ["Apache-2.0"]
# description = "A simple parser combinator library written in Gleam."
# repository = { type = "github", user = "username", repo = "project" }
# links = [{ title = 'GitHub', href = 'https://github.com/pd-andy/gleam-string-parser' }]

[dependencies]
gleam_stdlib = "~> 0.19"

[dev-dependencies]
gleeunit = "~> 0.6"